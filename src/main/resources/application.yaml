spring:
  application:
    name: spring-starter
  datasource:
    url: "jdbc:postgresql://localhost:5433/crm"
    username: postgres
    password: test
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    showSql: true
    hibernate:
      ddlAuto: validate
server:
  port: 8080